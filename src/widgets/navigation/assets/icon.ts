import { House, ShoppingCart, User, LucideIcon, BotMessageSquare } from 'lucide-react-native';

// type IconName = 'home' | 'search' | 'cart' | 'category' | 'my';
export const AppIcons: Record<string, LucideIcon> = {
    home: House,
    cart: ShoppingCart,
    my: User,
    customer: BotMessageSquare,
};
